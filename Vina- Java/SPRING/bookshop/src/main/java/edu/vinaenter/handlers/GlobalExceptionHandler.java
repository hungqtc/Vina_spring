package edu.vinaenter.handlers;

import org.springframework.web.bind.annotation.ControllerAdvice;
/**
 * @ControllerAdvice : Impact every controller if there is an error
 * Handling common exceptions that may occur for web app 
 */
@ControllerAdvice
public class GlobalExceptionHandler {
	
}
